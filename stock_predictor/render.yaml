services:
  - type: web
    name: stock-predictor
    runtime: python
    rootDir: stock_predictor
    buildCommand: pip install -r requirements.txt
    startCommand: python run.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
